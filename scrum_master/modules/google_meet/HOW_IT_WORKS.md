# Как работает модуль Google Meet

## 🎯 Общий принцип работы

Google Meet не предоставляет официального API для подключения ботов, поэтому мы используем **браузерную автоматизацию через Selenium**. Бот открывает реальный браузер (Chromium) и эмулирует действия пользователя.

## 📋 Процесс подключения (пошагово)

### 1. **Инициализация браузера**
```
GoogleMeetAdapter.initialize_driver()
```

Что происходит:
- Запускается Chromium в headless режиме (без GUI)
- Устанавливаются параметры для обхода детекции автоматизации
- Настраиваются разрешения для микрофона и камеры (fake media stream)
- Удаляется флаг `navigator.webdriver`

**Код:**
```python
options.add_argument('--use-fake-ui-for-media-stream')  # Автоматически разрешает доступ
options.add_argument('--use-fake-device-for-media-stream')  # Использует виртуальное устройство
options.add_experimental_option('excludeSwitches', ['enable-automation'])  # Скрывает автоматизацию
```

### 2. **Переход на страницу встречи**
```
driver.get(meet_url)
```

Что происходит:
- Браузер открывает ссылку типа `https://meet.google.com/xxx-yyyy-zzz`
- Google Meet загружает форму предварительного входа
- На странице появляются:
  - Превью камеры
  - Кнопки управления микрофоном и камерой
  - Поле ввода имени
  - Кнопка "Join now" или "Ask to join"

**Примерная структура HTML:**
```html
<div>
  <div aria-label="Turn off microphone">...</div>
  <div aria-label="Turn off camera">...</div>
  <input type="text" aria-label="Your name" />
  <button><span>Join now</span></button>
</div>
```

### 3. **Отключение микрофона и камеры**
```python
def disable_microphone_and_camera(self):
```

Что происходит:
- Ищем кнопку микрофона по селектору `div[aria-label="Turn off microphone"]`
- Если найдена - кликаем
- Ищем кнопку камеры по селектору `div[aria-label="Turn off camera"]`
- Если найдена - кликаем

**Зачем это нужно:**
- По умолчанию микрофон и камера включены
- Нам не нужна трансляция, только присутствие в комнате
- Экономим трафик и ресурсы

**Проблемы могут возникнуть если:**
- ❌ Google изменил структуру HTML
- ❌ Кнопки уже отключены (не критично, продолжаем)
- ❌ Локализация интерфейса (кнопки на другом языке)

### 4. **Ввод имени бота**
```python
def enter_display_name(self, name: str):
```

Что происходит:
- Ищем поле ввода по селектору `input[type="text"][aria-label="Your name"]`
- Очищаем поле (если там что-то было)
- Вводим имя бота через `send_keys(name)`

**Retry логика:**
- Пытаемся 5 раз с интервалом 1 секунда
- Проверяем `is_displayed()` и `is_enabled()` перед вводом
- Используем fallback селекторы если основной не работает

**Проблемы могут возникнуть если:**
- ❌ Пользователь уже залогинен в Google - поле может отсутствовать
- ❌ Поле еще не загрузилось (поэтому retry)
- ❌ Поле заблокировано модальным окном

### 5. **Нажатие кнопки "Join"**
```python
def click_join_button(self):
```

Что происходит:
- Ищем кнопку с текстом "Join now", "Ask to join" или "Join the call now"
- Используем XPath для поиска по тексту внутри span: `//button[.//span[text()="Join now"]]`
- Кликаем через JavaScript если обычный клик не работает

**Retry логика:**
- Пытаемся 10 раз с интервалом 2 секунды
- Проверяем видимость и доступность кнопки
- Если кнопка найдена но не кликается - используем `execute_script('arguments[0].click()')`

**Возможные варианты кнопок:**
- "Join now" - стандартная встреча
- "Ask to join" - встреча с модерацией
- "Join the call now" - некоторые варианты локализации

**Проблемы могут возникнуть если:**
- ❌ Встреча требует одобрения хоста (попадаем в waiting room)
- ❌ Модальное окно блокирует кнопку
- ❌ Кнопка еще не активна (все поля не заполнены)

### 6. **Ожидание загрузки комнаты**
```python
def wait_for_meeting_to_load(self):
```

Что происходит:
- Ждем появления элемента с `data-meeting-title` (заголовок встречи)
- Это означает что мы вошли в комнату
- Timeout: 30 секунд

**После успешного входа в комнате:**
- Видно других участников (или "You're the only one here")
- Активны кнопки управления (Leave, Mute, Camera)
- Можно видеть чат, участников, настройки

## 🔍 Селекторы и почему они работают

### Aria-label селекторы
```css
div[aria-label="Turn off microphone"]
```

**Почему используем:**
- ✅ Aria-labels стабильны (для доступности)
- ✅ Не зависят от классов/ID (которые могут быть минифицированы)
- ✅ Работают в разных версиях Google Meet
- ⚠️ Могут меняться при локализации

### XPath по тексту
```xpath
//button[.//span[text()="Join now"]]
```

**Почему используем:**
- ✅ Текст кнопок обычно стабилен
- ✅ Понятно что мы ищем
- ✅ Работает для динамически создаваемых элементов
- ⚠️ Зависит от языка интерфейса

## 🐛 Типичные проблемы и решения

### 1. "Microphone button not found"
**Причины:**
- Кнопка уже в состоянии "выключено"
- Google изменил aria-label
- Страница еще не загрузилась

**Решение:**
- ✅ Добавлены fallback селекторы
- ✅ Предупреждение не критично, продолжаем

### 2. "Could not enter display name"
**Причины:**
- Пользователь залогинен → имя берется из аккаунта
- Поле заблокировано или не интерактивно
- Модальное окно перекрывает поле

**Решение:**
- ✅ Retry логика с проверкой `is_enabled()`
- ✅ Если имя не введено, но есть кнопка Join → продолжаем

### 3. "Join button not found"
**Причины:**
- Встреча закончилась/не существует
- Требуется авторизация в Google
- Страница еще загружается

**Решение:**
- ✅ Множественные селекторы
- ✅ 10 попыток с ожиданием
- ❌ Если не найдена после всех попыток → ошибка

### 4. Попали в Waiting Room
**Что происходит:**
- Хост должен одобрить вход
- Бот ждет на странице "Waiting for host to let you in"

**Текущее поведение:**
- Бот считает что подключился (кнопка нажата)
- Статус: CONNECTED
- Реально в waiting room

**TODO:** Добавить детекцию waiting room

## 🔐 Ограничения и требования

### Google Meet не может:
- ❌ Записывать встречу напрямую (нужны дополнительные инструменты)
- ❌ Читать чат (доступ ограничен)
- ❌ Включать/выключать других участников
- ❌ Проходить 2FA без ручной авторизации

### Что может бот:
- ✅ Присоединиться к публичной встрече
- ✅ Отображаться как участник
- ✅ "Слушать" встречу (технически)
- ✅ Видеть количество участников
- ⚠️ Требует разрешения хоста для приватных встреч

## 🚀 Рекомендации по использованию

### 1. Время ожидания
```python
time.sleep(2)  # После каждого действия
```
**Зачем:** Элементы могут появляться с задержкой

### 2. Headless режим
```python
options.add_argument('--headless=new')
```
**Плюсы:** Работает на сервере без GUI
**Минусы:** Сложнее отладка

### 3. Fake Media Stream
```python
options.add_argument('--use-fake-device-for-media-stream')
```
**Зачем:** Chrome не спрашивает разрешение на камеру/микрофон

## 📊 Статусы встречи

```
PENDING → CONNECTING → CONNECTED
                    ↓
                  FAILED
                    ↓
              DISCONNECTED
```

- **PENDING**: Создана запись в БД, подключение не начато
- **CONNECTING**: Процесс подключения в процессе
- **CONNECTED**: Бот в комнате
- **FAILED**: Ошибка при подключении
- **DISCONNECTED**: Бот покинул комнату

## 🔧 Отладка

### Сохранить скриншот
```python
self.driver.save_screenshot('/tmp/debug.png')
```

### Вывести HTML страницы
```python
print(self.driver.page_source)
```

### Вывести все элементы с aria-label
```python
elements = self.driver.find_elements(By.CSS_SELECTOR, '[aria-label]')
for el in elements:
    print(f"aria-label: {el.get_attribute('aria-label')}")
```

### Отключить headless для визуального просмотра
```python
# В config.py или .env
GOOGLE_MEET_HEADLESS=false
```

## 📝 Пример полного цикла

```python
# 1. Пользователь делает запрос
POST /api/v1/meet/connect
{
  "meet_url": "https://meet.google.com/abc-defg-hij",
  "bot_name": "Scrum Bot"
}

# 2. Создается запись в БД (status=PENDING)
meeting = Meeting(user_id=..., meet_url=..., status=PENDING)

# 3. Обновляется статус (status=CONNECTING)
meeting.status = CONNECTING

# 4. Запускается Chromium
driver = webdriver.Chrome()

# 5. Открывается страница
driver.get("https://meet.google.com/abc-defg-hij")

# 6. Ожидание загрузки (2-3 сек)

# 7. Отключение микрофона/камеры
driver.find_element(By.CSS_SELECTOR, 'div[aria-label="Turn off microphone"]').click()
driver.find_element(By.CSS_SELECTOR, 'div[aria-label="Turn off camera"]').click()

# 8. Ввод имени
driver.find_element(By.CSS_SELECTOR, 'input[aria-label="Your name"]').send_keys("Scrum Bot")

# 9. Нажатие Join
driver.find_element(By.XPATH, '//button[.//span[text()="Join now"]]').click()

# 10. Ожидание входа в комнату
WebDriverWait(driver, 30).until(
    EC.presence_of_element_located((By.CSS_SELECTOR, 'div[data-meeting-title]'))
)

# 11. Обновление статуса (status=CONNECTED)
meeting.status = CONNECTED
meeting.connected_at = now()

# 12. Возврат ответа клиенту
return {"id": meeting.id, "status": "CONNECTED"}
```

## 🎓 Дополнительные материалы

- [Selenium Documentation](https://www.selenium.dev/documentation/)
- [Google Meet UI Components](https://material.io/components)
- [Web Accessibility (ARIA)](https://www.w3.org/WAI/ARIA/)
- [ChromeDriver Options](https://chromedriver.chromium.org/capabilities)
